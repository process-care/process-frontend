## ---- QUERIES

query questionsBySurveySlug($slug: String!) {
  questions(filters: { page: { survey: { slug: { eq: $slug } } } }) {
    data {
      id
      ...questionEntityFragment
    }
  }
}

## ---- MUTATUIONS

mutation createQuestion($values: QuestionInput!) {
  createQuestion(data: $values) {
    data {
      ...questionEntityFragment
    }
  }
}

mutation updateQuestion($id: ID!, $data: QuestionInput!) {
  updateQuestion(id: $id, data: $data) {
    data {
      id
      attributes {
        type
        internal_title
      }
    }
  }
}

mutation deleteQuestion($id: ID!) {
  deleteQuestion(id: $id) {
    data {
      id
    }
  }
}
