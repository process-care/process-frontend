query checkSurvey($surveyId: ID!) {
  checkSurvey(id: $surveyId) {
    valid
    errors {
      valid
      pageId
      errors {
        questionId
        __typename
        unordered {
          conditionId
          targetId
        }
      }
    }
  }
}